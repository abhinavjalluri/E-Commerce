server:
  port: 8083
spring:
  application:
    name: inventory-service
  profiles:
    active: docker
springdoc:
  api-docs:
    path: /api-docs
management:
  endpoints:
    web:
      exposure:
        include: health,metrics

