server:
  port: 8081
spring:
  application:
    name: user-service
  profiles:
    active: docker
springdoc:
  api-docs:
    path: /api-docs
management:
  endpoints:
    web:
      exposure:
        include: health,metrics